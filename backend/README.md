# Backend

Node.js + Express + PostgreSQL backend for UAT Session Tracker.

## Commands

```powershell
npm install
npm run dev
npm run check
```

## Environment

Create `.env` from `.env.example`.

Never commit real database credentials.
